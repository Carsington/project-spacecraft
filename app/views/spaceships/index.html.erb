<h1 class="mb-3">Discover the best spacecrafts accross the galaxy</h1>

<% @spaceships.each do |spaceship| %>
  <div class="card-ship m-2">
    <%= link_to "Book Now", new_spaceship_booking_path(spaceship), class: 'mybuttonoverlap btn btn-info' %>
    <img src=<%= "#{spaceship.main_pic_url}" %> />
    <div class="card-ship-infos">
      <div>
        <h2><%= spaceship.name %></h2>
        <p><%= spaceship.description.first(80) %></p>
      </div>
      <h2 class="card-ship-pricing"><%= spaceship.unit_price %>€</h2>
      <img src="https://pbs.twimg.com/profile_images/794107415876747264/g5fWe6Oh_400x400.jpg" class="card-ship-user avatar-bordered" />
    </div>
  </div>
<% end %>

<!--   <div class="spaceships-list">
    <% @spaceships.each do |spaceship| %>
      <%= link_to spaceship_path(spaceship) do %>
        <div class="card-spaceship">
          <%= image_tag spaceship.main_pic_url, class: "spaceship-img", alt: "spaceship", width: 350 %>
          <div class="card-spaceship-info">
            <div>
              <h2><%= spaceship.name %></h2>
              <p><%= spaceship.description %></p>
            </div>
            <h2 class="card-spaceship-pricing"><%= spaceship.unit_price %>$</h2>
            <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-spaceship-user avatar-bordered" />
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div> -->


<!-- ---------- DO NOT TOUCH ---------- -->
<!-- <div class="container">
  <h1>Spaceships index</h1>

  <div class="spaceships-list">
    <% @spaceships.each do |spaceship| %>
      <%= link_to spaceship_path(spaceship) do %>
      <div class="card-spaceship">
        <h3><%= spaceship.name %></h3>
        <p><%= spaceship.unit_price %> units/day</p>
      </div>
      <% end %>
    <% end %>
  </div>
</div> -->
<!-- ---------------------------------- -->
