<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">

  <%= link_to image_tag("fusee", alt: "alttext", class: 'avatar-bordered'), root_path, class: 'navbar-brand' %>

  <%= render 'shared/search-bar' %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="links">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">

      <ul class="navbar-nav mr-auto">

        <li class="nav-item active">
          <%= link_to "Add a spaceship", new_spaceship_path, class:'nav-link' %>
        </li>

        <li class="nav-item active">
          <% if user_signed_in? == false %>
          <%= link_to "Sign-up", new_user_registration_path, class:'nav-link' %>
          <% end %>
        </li class="nav-item">

        <li class="nav-item active">
          <% if user_signed_in? == false %>
          <%= link_to "Login", new_user_session_path, class:'nav-link' %>
          <% end %>
        </li>

        <li class="nav-item active">
          <% if user_signed_in? %>
          <%= link_to "Dashboard", user_path(current_user), class:'nav-link' %>
          <% end %>
        </li>

        <% if user_signed_in? %>
        <img class="avatar dropdown-toggle" id="navbarDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" src="<%= current_user.photo.attached? ? current_user.photo.service_url : "https://pbs.twimg.com/profile_images/794107415876747264/g5fWe6Oh_400x400.jpg" %>" />
        <li class="nav-item dropdown">
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%#= link_to "My profile", edit_user_path(current_user), class:'dropdown-item' %>

            <%= link_to "My profile", edit_user_registration_path(current_user), class:'dropdown-item' %>
            <%= link_to "Sign-out", destroy_user_session_path, method: :delete, class:'dropdown-item' %>
          </div>
        </li>
        <% end %>

      </ul>
    </div>
  </div>
</div>
